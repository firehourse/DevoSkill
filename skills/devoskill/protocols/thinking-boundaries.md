# Thinking Boundaries Protocol

Use this protocol when deciding which constraints must be confirmed with the user instead of guessed.

## Must Confirm
- schema or contract details
- externally owned API behavior
- production constraints
- immovable module boundaries
- whether patterns must be preserved or can change
- what phrase or approval counts as implementation authorization

If code-change approval is not explicit, default to planning-only.
